const minutesDisplay = document.querySelector(".minutes")
const secondsDisplay = document.querySelector(".seconds")
const btnPlay = document.querySelector(".play")
const btnPause = document.querySelector(".pause")
const btnStop = document.querySelector(".stop")
const btnIncrement = document.querySelector(".more")
const btnDecrement = document.querySelector(".less")
const playForest = document.querySelector(".play-forest")
const stopForest = document.querySelector(".stop-forest")
const playRain = document.querySelector(".play-rain")
const stopRain = document.querySelector(".stop-rain")
const playCoffeeMachine = document.querySelector(".play-coffee-machine")
const stopCoffeeMachine = document.querySelector(".stop-coffee-machine")
const playFireplace = document.querySelector(".play-fireplace")
const stopFireplace = document.querySelector(".stop-fireplace")


export {
  minutesDisplay,
  secondsDisplay,
  btnPlay,
  btnPause,
  btnStop,
  btnIncrement,
  btnDecrement,
  playForest,
  stopForest,
}
